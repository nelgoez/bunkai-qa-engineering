# ADR-NNNN: <Short title>

- Status: Proposed
- Date: YYYY-MM-DD
- Supersedes: —
- Superseded by: —

## Context

Why this decision matters. The state of the system that forced the choice, and the
constraint it resolves. Reference the tickets / commits / incidents that motivated it.

## Decision

What was decided, precisely. Names, config values, file locations, so the reader can verify
the suite actually follows it.

## Consequences

### Positive

What this buys (runtime, isolation, readability, trust).

### Negative / trade-off

What it costs (setup latency, coupling, brittleness, lock-in). A decision with no downside
is under-examined — name the real cost.

## Alternatives considered

Each rejected option + one line on why it lost.
